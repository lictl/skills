interface:
  display_name: "GitHub Orchestrate"
  short_description: "Delegate issue-scoped work through reviewed pull requests"
  default_prompt: "Use $github-orchestrate to delegate and review this task through GitHub, requiring my review before merging ADR or spec changes."
