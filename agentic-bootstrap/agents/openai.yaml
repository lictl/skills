interface:
  display_name: "Agentic Bootstrap"
  short_description: "Prepare local workflow setup for human review and commit"
  default_prompt: "Use $agentic-bootstrap to prepare this project's workflow locally, leaving changes uncommitted for my review and commit."
